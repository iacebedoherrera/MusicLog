# Public URL of the API. In production behind the same reverse proxy, keep /api.
VITE_API_URL=/api

# Development-only Vite proxy target. It is not bundled into the production build.
VITE_PROXY_TARGET=http://localhost:8080
